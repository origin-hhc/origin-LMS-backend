const { createSubRole } = require("./createSubRole");
const { deleteSubRole } = require("./deleteSubRole");
const { getSubRoles } = require("./getSubRoles");
const { updateSubRole } = require("./updateSubRole");

module.exports = {
  getSubRoles,
  createSubRole,
  updateSubRole,
  deleteSubRole,
};
